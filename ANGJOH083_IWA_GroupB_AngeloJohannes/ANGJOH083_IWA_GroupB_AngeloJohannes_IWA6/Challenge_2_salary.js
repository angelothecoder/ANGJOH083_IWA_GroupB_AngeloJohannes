const rent = 400;
const tax = 8 / 100;
const food = 51.7501;
const salary = 800;
const transport = 10.2;
const hourOfDay = 00;
const minuteOfDay = 00;

// Only change below this line

if (hourOfDay && minuteOfDay !== null) {

  const startingAfterTax = salary - tax;
  const balace = starting - rent - food - transport;
} 
else if (hourOfDay && minuteOfDay !== '00'){
    const startingAfterTax = salary - tax;
    const balace = starting - rent - food - transport;
}
	
console.log('R' + balance.toFixed(2));